# Work In Progress

Active work being done by agents. Each section is removed once the branch is merged.

---

_No active work — PR #96 (Vault fix) merged, migration applied to hosted Supabase, reconcile_subscription now reads vault.decrypted_secrets. Pending operator action: add 2 secrets to Project Settings → Vault (edge_functions_url, service_role_key) per docs/phase-16a-setup.md §2. Next code work: Phase 16b (client integration + paywall UI + onboarding rewire)._
