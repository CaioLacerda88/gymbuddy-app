# Work In Progress

Active work being done by agents. Each section is removed once the branch is merged.

---

*Last merged: PR #61 (refactor(auth): route currentUser.id through currentUserIdProvider — W6, Phase 13 Sprint C).*
