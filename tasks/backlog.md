# Backlog — product/UX follow-ups

Non-blocker items discovered outside of an active PLAN.md step. Each entry is a future ticket: small enough to scope in a single PR, big enough to not get lost. When pulled into active work, move the spec to PLAN.md and delete the backlog entry here.

---

(empty — all items resolved or deferred to PLAN.md phases)

- **BL-1**: CLOSED — folded into BL-3 (#71), shipped as acceptance #14 (workout-count vs point-count disambiguation).
- **BL-2**: DEFERRED — captured as Phase 15e observation in PLAN.md (home active-plan dead space; do not pre-design without gamification scope lock).
- **BL-3**: CLOSED — PR #71 merged (commit `966505f`). Full progress chart rebuild: e1RM primary, 30d default, PR gold ring, labeled Y/X axes, trend copy with BL-1 fix, density-aware rendering, container chrome. Tests: 1143 total.
